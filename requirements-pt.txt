# Dependencies for the PyTorch / transformers serve image (Dockerfile.pt).
#
# Install (CUDA 12.4 wheels for torch/torchvision/triton):
#   pip install -r requirements-pt.txt \
#     --index-url https://download.pytorch.org/whl/cu124 \
#     --extra-index-url https://pypi.org/simple
#
# Version lock notes:
# - torch 2.6.0 ↔ torchvision 0.21.0 ↔ triton 3.2.0 (must stay in sync)
# - transformers 4.57.x uses autoawq for AWQ (not gptqmodel; that needs torch>=2.8)

# --- PyTorch CUDA 12.4 ---
torch==2.6.0
torchvision==0.21.0
triton==3.2.0

# --- HF serving (OpenAI-compatible transformers serve) ---
transformers[serving,sentencepiece]==4.57.6
accelerate==1.12.0
huggingface-hub==0.36.0
tokenizers==0.22.1
safetensors==0.5.3
sentencepiece==0.2.1
protobuf==5.29.5
pillow==11.3.0

# --- AWQ ---
autoawq==0.2.9
